<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"                    
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                    mc:Ignorable="d">

    <Style TargetType="{x:Type GameDetails}">
        <Setter Property="FocusManager.FocusedElement" Value="{Binding ElementName=PART_ButtonContext}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type GameDetails}">
                    <Grid
                          x:Name="PART_ViewHost"
                          d:DesignWidth="{StaticResource DesignWidth}"
                          d:DesignHeight="{StaticResource DesignHeight}"
                          d:DataContext="{x:Static DesignMainViewModel.DesignSelectedGameIntance}"
                          KeyboardNavigation.DirectionalNavigation="Contained"
                          KeyboardNavigation.TabNavigation="Contained">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition Width="2*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="2*" />
                            <RowDefinition Height="6*" />
                        </Grid.RowDefinitions>

                        <Border x:Name="Border" BorderThickness="4" BorderBrush="{DynamicResource SelectionBrush}"
                                Margin="0" Visibility="Collapsed" Grid.ColumnSpan="2" CornerRadius="10" Panel.ZIndex="17" Width="500" Height="496" RenderTransformOrigin="0.5,0.5" Grid.Row="1">
                            <Border.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform/>
                                    <SkewTransform/>
                                    <RotateTransform/>
                                    <TranslateTransform X="107"/>
                                </TransformGroup>
                            </Border.RenderTransform>
                        </Border>
                        <TextBlock x:Name="PART_NomJeux" 
                            RenderOptions.BitmapScalingMode="Fant" RenderTransformOrigin="0.5,0.5"  ScrollViewer.VerticalScrollBarVisibility="Disabled" UseLayoutRounding="False"  Grid.Row="0" Grid.ColumnSpan="3" FontSize="36" HorizontalAlignment="Center" Margin="4,-74.5,-4,192.5" Height="150
                                   "  TextAlignment="Center" Grid.Column="0" Grid.RowSpan="1" Panel.ZIndex="3" FontWeight="Bold" Background="{DynamicResource Black}" TextOptions.TextHintingMode="Fixed" VerticalAlignment="Bottom" Padding="0,85,0,0" FontFamily="{DynamicResource Comfortaa}" Text="{Binding DisplayName}" Width="1920">
                            <TextBlock.Style>
                                <Style>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementName=PART_ElemGameDetails, Path=Visibility}" Value="Visible">
                                            <DataTrigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <ColorAnimation Storyboard.TargetProperty="Foreground.(SolidColorBrush.Color)" Duration="0:0:0" From="White" To="Black"/>
                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" Duration="0:0:0.4" From="0" To="1"/>
                                                        <ColorAnimation Storyboard.TargetProperty="Foreground.(SolidColorBrush.Color)" Duration="0:0:0.2" From="Black" To="White" BeginTime="0:0:0.5"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.EnterActions>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <Border x:Name="GrandeBordure" BorderThickness="5" BorderBrush="white" Style="{DynamicResource ApparitionFonduGamedetail}" Background="Black"
                                Margin="-188.25,-73.75,106.25,777" Grid.ColumnSpan="3" Grid.RowSpan="2" Panel.ZIndex="2" Width="Auto" Height="Auto" RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform ScaleY="-1"/>
                                    <SkewTransform/>
                                    <RotateTransform/>
                                    <TranslateTransform X="142"/>
                                </TransformGroup>
                            </Border.RenderTransform>
                        </Border>

                        <Border  BorderThickness="3.81" BorderBrush="{DynamicResource SelectionBrush}" RenderOptions.BitmapScalingMode="Fant" Grid.ColumnSpan="4" CornerRadius="12" Grid.RowSpan="2" Panel.ZIndex="4" Height="225" RenderTransformOrigin="0.5,0.5" Margin="0,-725,0,0" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Border.Background>
                                <ImageBrush ImageSource="{Binding Game.CoverImage, Converter={StaticResource ImageStringToImageConverter}}"></ImageBrush>
                            </Border.Background>

                            <Image RenderOptions.BitmapScalingMode="NearestNeighbor" Visibility="Hidden"  Source="{Binding Game.CoverImage, Converter={StaticResource ImageStringToImageConverter}}" Grid.RowSpan="1" Stretch="Uniform" Panel.ZIndex="-1" RenderTransformOrigin="0.5,0.5" Grid.ColumnSpan="3" Opacity="0"/>
                        </Border>
                        <StackPanel Orientation="Horizontal" Panel.ZIndex="2" Margin="-3586,0,81,0" Grid.ColumnSpan="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="650" RenderTransformOrigin="0.5,0.5" Grid.Column="2" Height="65">
                            <StackPanel.Style>
                                <Style>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementName=PART_ElemGameDetails, Path=Visibility}" Value="Visible">
                                            <DataTrigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <ThicknessAnimation Storyboard.TargetProperty="Margin" Duration="0:0:0.2" From="-3586,0,81,0" To="-1980,0,81,0"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.EnterActions>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <StackPanel Margin="100,0,0,0" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                    FontFamily="{DynamicResource Comfortaa}"
                                    Text="{DynamicResource LOCTimePlayed}"
                                    Margin="0,0,0,10" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                    Text="{Binding Playtime, Converter={StaticResource PlayTimeToStringConverter}}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </StackPanel>

                            <StackPanel Margin="100,0,0,0" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                    FontFamily="{DynamicResource Comfortaa}"
                                    Text="{DynamicResource LOCGameLastActivityTitle}"
                                    Margin="0,0,0,10" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                    Text="{Binding LastActivity, Converter={StaticResource DateTimeToLastPlayedConverter}}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Panel.ZIndex="2" DockPanel.Dock="Top" Margin="1301,0,2,0" Grid.ColumnSpan="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="650" RenderTransformOrigin="0.5,0.5" Grid.Column="2" Height="65">
                            <StackPanel.Style>
                                <Style>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementName=PART_ElemGameDetails, Path=Visibility}" Value="Visible">
                                            <DataTrigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <ThicknessAnimation Storyboard.TargetProperty="Margin" Duration="0:0:0.2" From="1301,0,2,0" To="150,0,2,0"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.EnterActions>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <StackPanel Margin="50,0,0,0" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                               FontFamily="{DynamicResource Comfortaa}"
                                               Text="{DynamicResource LOCGameReleaseDateTitle}"
                                               Margin="0,0,0,10" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                               Text="{Binding ReleaseDate}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </StackPanel>

                            <StackPanel Margin="50,0,0,0" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                               FontFamily="{DynamicResource Comfortaa}"
                                               Text="{DynamicResource LOCGamePlatformTitle}"
                                               Margin="0,0,0,10" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                               Text="{Binding Platforms, Converter={StaticResource ListToStringConverter}}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </StackPanel>
                        </StackPanel>
                        <!-- <Border x:Name="Description" Background="{DynamicResource TransparentDark}" BorderBrush="White" BorderThickness="3" RenderTransformOrigin="0.5,0.5" Width="Auto" Height="Auto" Margin="240,88,50,90" CornerRadius="15" Padding="50,0" Grid.RowSpan="1" Grid.Column="2" Grid.Row="1" HorizontalAlignment="Right">

                            <HtmlTextView x:Name="PART_HtmlDescription"
                                          HtmlFontSize="16"
                                          HtmlForeground="{DynamicResource TextColor}"
                                          LinkForeground="{DynamicResource GlyphColor}"
                                          FocusVisualStyle="{x:Null}"
                                          Focusable="False"
                                          Visibility="Visible"
                                          Margin="0,20,0,0">
                            </HtmlTextView>
                        </Border> -->

                        <TextBlock Name="VideoPathMicro" Tag="{DynamicResource ExtraMetadataPath}" Visibility="Collapsed" d:IsLocked="True">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text">
                                        <Setter.Value>
                                            <MultiBinding StringFormat="{}{0}{1}{2}{3}">
                                                <Binding RelativeSource="{RelativeSource Self}" Path="Tag" />
                                                <Binding Source="\ExtraMetadata\games\"/>
                                                <Binding Path="Game.Id"/>
                                                <Binding Source="\VideoTrailer.mp4" />
                                            </MultiBinding>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <MediaElement MediaElementBehaviors.Repeat="False" x:Name="VideoSourceBackgrounds" Tag="{DynamicResource UseAbsoluteExtraMetadataPath}" Volume="0" HorizontalAlignment="Center" Height="1080" VerticalAlignment="Top" Margin="0,-125,0,70" Grid.Row="1" Stretch="UniformToFill" StretchDirection="Both" Grid.ColumnSpan="3" Panel.ZIndex="1">
                            <MediaElement.Style>
                                <Style TargetType="MediaElement">
                                    <Setter Property="MediaElement.Source" Value="{Binding ElementName=VideoPathMicro, Path=Text}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}, Path=IsActive}" Value="False">
                                            <Setter Property="MediaElement.Source" Value="" />
                                        </DataTrigger>
                                        <DataTrigger  Binding="{Binding ElementName=PART_ElemGameDetails, Path=Visibility}" Value="Visible">
                                            <DataTrigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="0"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.EnterActions>
                                        </DataTrigger>
                                        <EventTrigger RoutedEvent="MediaEnded">
                                            <EventTrigger.Actions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Duration="0:0:1" Storyboard.TargetProperty="Opacity" From="1" To="0"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger.Actions>
                                        </EventTrigger>
                                        <EventTrigger RoutedEvent="MediaFailed">
                                            <EventTrigger.Actions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Duration="0" Storyboard.TargetProperty="Opacity" To="0"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger.Actions>
                                        </EventTrigger>
                                        <EventTrigger RoutedEvent="MediaOpened">
                                            <EventTrigger.Actions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation BeginTime="0:0:3" Duration="0:0:1" Storyboard.TargetProperty="Opacity" To="1"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger.Actions>
                                        </EventTrigger>
                                    </Style.Triggers>
                                </Style>
                            </MediaElement.Style>
                        </MediaElement>

                        <FadeImage x:Name="PART_ImageBackground" Grid.RowSpan="1" RenderOptions.BitmapScalingMode="HighQuality" Style="{DynamicResource ApparitionFonduGamedetail}"
                                       HorizontalAlignment="Center" VerticalAlignment="Top" Height="707" Margin="0,33,0,0" Panel.ZIndex="0" Grid.Row="1" Stretch="UniformToFill" Grid.ColumnSpan="3"/>

                        <DockPanel Grid.Column="0" Grid.Row="1"
                                   Margin="-35,90,0,10"
                                   HorizontalAlignment="Left" Panel.ZIndex="1" Grid.ColumnSpan="3" Width="1955" >


                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,25,20,20" />
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Height="80" HorizontalAlignment="Center" VerticalAlignment="Bottom" Panel.ZIndex="2" Margin="0,0,0,-90" Width="Auto">
                                <StackPanel.Style>
                                    <Style>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=PART_ElemGameDetails, Path=Visibility}" Value="Visible">
                                                <DataTrigger.EnterActions>

                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <ThicknessAnimation Storyboard.TargetProperty="Margin" Duration="0:0:0.3" From="0,0,0,-90" To="0,0,0,65"/>
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" Duration="0:0:0.4" From="0" To="1"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.EnterActions>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>
                                <ButtonEx x:Name="PART_ButtonContext" Style="{DynamicResource ButtonDetail}" TabIndex="0"
                                          Margin="2" BorderThickness="4" MinWidth="220" HorizontalContentAlignment="Center" FontSize="36"  FontWeight="Bold" FocusVisualStyle="{x:Null}" HorizontalAlignment="Right" />
                                <ButtonEx x:Name="PART_ButtonOptions" Style="{DynamicResource ButtonDetail}"
                                          Content="..."
                                          Margin="5,2,2,2" BorderThickness="4" MinWidth="25" HorizontalContentAlignment="Center" Width="75" HorizontalAlignment="Right" FontSize="36" Height="75" VerticalContentAlignment="Center" FontWeight="Bold" FocusVisualStyle="{x:Null}"/>
                            </StackPanel>
                        </DockPanel>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>